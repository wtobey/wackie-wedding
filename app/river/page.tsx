import RiverScene from "@/components/RiverScene";

// The new whimsical, low-poly, aerial river — a second full-screen "home" that
// lives at its own URL (/river) alongside the ported polaroid home at /.
export default function RiverPage() {
  return <RiverScene />;
}
